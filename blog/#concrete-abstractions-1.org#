#+TITLE:       [draft]Concrete Abstractions―属于凡人的计算过程(1)
#+AUTHOR:      aunmro
#+EMAIL:       aunmro@qq.com
#+DATE:        2018-03-25 Sun
#+URI:         /blog/%y/%m/%d/concrete-abstrations-1/
#+KEYWORDS:    scheme
#+TAGS:        scheme
#+LANGUAGE:    zh-Hans
#+OPTIONS:     H:3 num:nil toc:nil \n:nil ::t |:t ^:nil -:nil f:t *:t <:t
#+DESCRIPTION: Concrete Abstractions

* Part 1 过程抽象

** Chapter 1 计算机科学与编程

** 计算过程

计算机科学是围绕计算过程的科学。当计算机忙于做某些事时，一个过程就在它的内部进行。计算过程远不止算术计算那么简单，那么什么是计算过程？

举个例子：当你用字典查阅 fiduciary 这个词的意思时

1. 根据字典的索引确定f的位置
2. 再根据i确定 fi 的位置
3. 接下来直接向后查找 fiduciary

以上就是一个过程。关于计算过程，有很多疑问：

+ 如何描述一个过程
+ 如何证明计算过程达到了特殊的效果
+ 几个产生相同计算结果的过程，如何选择一个最好的
+ 如何构建一台自动执行指定过程的机器

等等，接下来我们将讨论这些问题。

** Scheme
  
描述计算过程需要一门编程语言，这里我们将使用 Scheme(/Racket/) 语言。

最简单的 Scheme 程序是一个数字，输入一个数字，Scheme 将返回这个数字。

#+BEGIN_SRC racket
#lang racket
12
;; 12
#+END_SRC

同样可以输入负数，分数，小数。Scheme 中，小数是不精确的近似值，分数是精确的有理数。

#+BEGIN_SRC racket
#lang racket
-7
;; - 7
1/3
;; 1/3
3.1415927
;; 3.1415927
#+END_SRC

Scheme 预先定义了很多过程，输入过程名过程名即可查阅，比如 =+= =sqrt=

#+BEGIN_SRC racket
#lang racket
sqrt
;; #<procedure:sqrt>
+
;; #<procedure:+>
#+END_SRC

应用一个过程：

#+BEGIN_SRC racket
#lang racket
(sqrt 9) ;;3
(+ 3 6) ;;9
#+END_SRC

过程由一个用括号包裹的表达式列表组成。 第一个表达式的值是要应用的过程; 剩下的表达式的值是作为应用过程的参数。 应用程序本身就是表达式，因此它们可以嵌套：

#+BEGIN_SRC racket
#lang racket
(sqrt (+ 3 6)) ;;3
#+END_SRC

计算过程

#+BEGIN_SRC racket
(+ 3 6) = 9
(sqrt 9) ;;3
#+END_SRC

一些其他的计算过程: =+= =-= =*= =/=

#+BEGIN_QUOTE
练习1.1

用 Scheme 计算下列值

#+BEGIN_SRC racket
#lang racket
(* 3 4)
(* (+ 5 3) (- 5 3))
(/ (+ (* (- 17 14) 5) 6) 7)
#+END_SRC

#+RESULTS:
: 12
: 16
: 3
#+END_QUOTE

*复杂的表达式可以通过换行和缩进来阐明其结构* ，比如练习1.1的 =(/ (+ (* (- 17 14) 5) 6) 7)= ,可以改为：

#+BEGIN_SRC racket
#lang racket
(/ (+ (* (-17 14)
         5)
      6)
   7)
#+END_SRC

我们还需要学会为过程定义名称：

#+BEGIN_SRC racket
#lang racket
(define ark-volume (* (* 300 50) 30))
ark-volume ;; 450000
#+END_SRC

Scheme 解释器首先计算 =(* (* 300 50) 30)= 的值，得到 450000, 然后 =ark-volume= 便成了这个值的名称，你可以运行这个过程。

仅仅为计算结果命名还不够。例如，假设我们要计算几个不同项目的成本，包括5％的销售税，我们可以考虑每件商品的价格，计算销售税，并将税额加到原价中：

#+BEGIN_SRC racket
#lang racket
(+ 1.29 (* 5/100 1.29))
;; 1.3545
(+ 2.40 (* 5/100 2.40))
;; 2.52
#+END_SRC

我们可以定义一个类似 =sqrt= 的过程，通过产品的价格，计算总成本。Scheme 里，通过 *lambda* 表达式指定一种计算方法：

#+BEGIN_SRC racket
#lang racket
(lambda (x) (+ x (* 5/100 x)))
;; #<procedure>
((lambda (x) (+ x (* 5/100 x))) 1.29)
;; 1.3545
((lambda (x) (+ x (* 5/100 x))) 2.40)
;; 2.52
#+END_SRC


